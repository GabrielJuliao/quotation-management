#server
server.port=${SERVER_PORT}
server.servlet.context-path=/api

#datasource
spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USERNAME}
spring.datasource.password=${DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#jpa
#2 NOTE: NOT IDEAL FOR NON EMBEDDED DATABASES.
spring.datasource.initialization-mode=always
spring.jpa.open-in-view=false
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

## App Properties
app.stock-manager.stock-url=${STOCK_MANAGER_STOCK_URL}
app.stock-manager.register-url=${STOCK_MANAGER_NOTIFICATION_URL}
app.stock-manager.register-host=${STOCK_MANAGER_NOTIFICATION_HOST}
app.stock-manager.register-port=${STOCK_MANAGER_NOTIFICATION_PORT}